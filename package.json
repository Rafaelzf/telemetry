{
  "private": true,
  "name": "telemetry-monorepo",
  "workspaces": [
    "back",
    "sdk",
    "front"
  ],
  "scripts": {
    "build:front": "npm run build --workspace=sdk && npm run build --workspace=front"
  }
}
